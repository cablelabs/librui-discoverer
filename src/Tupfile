include_rules

LIBNAME = rui-discoverer

: *.vala |> !vala --vapi=$(LIBNAME).vapi --library $(LIBNAME) -H $(LIBNAME).h -X -fPIC -X -shared -o lib$(LIBNAME).so |> $(LIBNAME).h $(LIBNAME).vapi lib$(LIBNAME).so
